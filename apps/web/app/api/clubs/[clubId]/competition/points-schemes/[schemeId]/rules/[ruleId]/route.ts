import {NextRequest,NextResponse} from 'next/server';import {authorizeCompetitionSeries} from '@/features/competition/series/competition-series.auth';import {rpc} from '@/features/competition/points-schemes/points-schemes.repository';import {integer,object,revision} from '@/features/competition/points-schemes/points-schemes.validation';import {pointsError} from '@/features/competition/points-schemes/points-schemes.http';type C={params:Promise<{clubId:string;schemeId:string;ruleId:string}>};export async function PATCH(req:NextRequest,c:C){const {clubId,schemeId,ruleId}=await c.params;const auth=await authorizeCompetitionSeries(req,clubId,'write');if(auth.error||!auth.client)return auth.error;try{const v=object(await req.json());return NextResponse.json({rule:await rpc(auth.client,'update_points_scheme_rule',{p_club_id:clubId,p_scheme_id:schemeId,p_rule_id:ruleId,p_scheme_revision:revision(v.scheme_revision),p_rule_revision:revision(v.rule_revision),p_points:integer(v.points,'POINTS'),p_sort_order:integer(v.sort_order,'SORT_ORDER'),p_is_active:Boolean(v.is_active)})})}catch(e){return pointsError(e)}}export async function DELETE(req:NextRequest,c:C){const {clubId,schemeId,ruleId}=await c.params;const auth=await authorizeCompetitionSeries(req,clubId,'write');if(auth.error||!auth.client)return auth.error;try{const v=object(await req.json());return NextResponse.json({rule:await rpc(auth.client,'deactivate_points_scheme_rule',{p_club_id:clubId,p_scheme_id:schemeId,p_rule_id:ruleId,p_scheme_revision:revision(v.scheme_revision),p_rule_revision:revision(v.rule_revision)})})}catch(e){return pointsError(e)}}
